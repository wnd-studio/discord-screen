{
  "name": "discord-screen",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "workspaces": ["client"],
  "scripts": {
    "dev": "wrangler dev",
    "build": "node scripts/build-worker.mjs",
    "check": "wrangler deploy --dry-run",
    "test:webhook": "node scripts/test-discord-webhook.mjs",
    "smoke": "node scripts/smoke-worker.mjs",
    "deploy": "pnpm build && wrangler deploy",
    "cf:typegen": "wrangler types"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "wrangler": "^4.33.1"
  }
}
